services:
  - type: web
    name: agri-vision-ai-api
    env: python
    buildCommand: pip install -r requirements-prod.txt
    startCommand: uvicorn src.app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ALLOWED_ORIGINS
        value: https://agri-vision-ai.vercel.app
